import 'package:flutter/material.dart';

// define the theme for the app

//define set of colors
const Color backgroundColor = Color.fromRGBO(127, 0, 0, 1.0);
const Color secondaryColor = Color.fromRGBO(134, 132, 132, 1.0);
const Color tertiaryColor = Color.fromRGBO(218, 22, 13, 1.0);
const Color checkBox = Color.fromRGBO(112, 0, 0, 1.0);
const Color buttonColor = Color.fromRGBO(165, 60, 55, 1.0);
const Color buttonColorMenu = Color.fromRGBO(255, 254, 254, 1.0);